# Story Plotter - Environment Variables
# Copy this file to .env.local and fill in your values

# ===================
# Authentication (NextAuth.js)
# ===================
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# ===================
# Cloudflare R2 Storage (handles ALL data - users, projects, files)
# ===================
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=story-plotter
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# Note: Files are served through API proxy (/api/files/[...key]) for security
# Direct R2 public URLs are NOT used to prevent browser trust issues

# ===================
# Application Settings
# ===================
# Maximum file upload size in bytes (default: 10MB)
MAX_UPLOAD_SIZE=10485760

# Auto-save interval in milliseconds (default: 30000 = 30 seconds)
AUTO_SAVE_INTERVAL=30000

# Maximum number of version snapshots to keep per project (default: 50)
MAX_VERSION_HISTORY=50

# ===================
# Feature Flags (Optional)
# ===================
# Enable/disable features
# ENABLE_REGISTRATION=true
# ENABLE_PASSWORD_RESET=true
# ENABLE_FILE_UPLOADS=true
