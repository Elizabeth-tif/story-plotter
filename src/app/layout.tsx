import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Story Plotter - Plan Your Stories',
  description: 'A powerful story planning application for writers. Organize characters, scenes, plotlines, and more.',
  keywords: ['story planning', 'writing', 'novel', 'screenwriting', 'characters', 'plotlines'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
